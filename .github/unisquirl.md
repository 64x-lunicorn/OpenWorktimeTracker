# Unisquirl config

Written by `/setup`. Edit by hand any time.

## Issue tracker

**GitHub Issues** on `64x-lunicorn/OpenWorktimeTracker` — see
`.github/reference/issue-tracker-github.md` for the operations.
